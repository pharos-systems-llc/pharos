# ========================================================================
# Project: pharos
# Component: Shared Client Library (pharos-client)
# File: crates/pharos-client/Cargo.toml
# Author: Richard D. (https://github.com/iamrichardd)
# License: AGPL-3.0 (See LICENSE file for details)
# * Purpose (The "Why"):
# This crate provides a shared, async-first client library for interacting
# with a Pharos server using the RFC 2378 protocol.
# * Traceability:
# Related to Task 10.1 (Issue #39)
# ========================================================================

[package]
name = "pharos-client"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.37", features = ["full"] }
tokio-util = { version = "0.7", features = ["codec"] }
futures = "0.3"
ssh-key = { version = "0.6", features = ["ed25519"] }
ed25519-dalek = "2.2"
signature = "2.2"
base64 = "0.22"
hex = "0.4"
anyhow = "1.0"
log = "0.4"
tracing = "0.1"
tokio-rustls = { version = "0.26", features = ["ring"] }
rustls-pki-types = "1.15"
webpki-roots = "0.26"
rustls-native-certs = "0.8"
