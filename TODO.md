# Pharos Project Backlog

## Definition of Done (DoD)
A task is considered complete and may be marked `[x]` only when:
1. **Local Verification:** All tests/builds pass inside the `Containerfile.test` Podman environment.
2. **Standardization:** All new files include the Standardized File Prologue.
3. **CI Validation:** GitHub Actions (CI) pass successfully on all target platforms (`linux`, `macos`, `windows`).
4. **Production Verification:** The change is verified to be live and functional on the production environment (e.g., `https://iamrichardd.com/pharos/`) using `web_fetch` or manual verification.
5. **AI-Handover:** The GitHub Issue is closed with a structured "Fix Summary" and an "AI-Ready Verification Prompt."
6. **State Sync:** `@PROGRESS.md` and `@TODO.md` are updated.

## Phase 1: Zero-Host & DevSecOps Foundation
- [x] **Task 1.1 (Issue #1):** Initialize internal project directory structure within the current root (create workspaces for `pharos-server` backend, `ph` CLI, and `mdb` CLI).
- [x] **Task 1.2 (Issue #2):** Create `Containerfile.test` and `Containerfile.debug` for Podman-based Zero-Host execution.
- [x] **Task 1.3 (Issue #3):** Select primary programming language and initialize dependency management inside the `Containerfile.debug` environment.
- [x] **Task 1.4 (Issue #4):** Create `SECURITY.md` detailing DevSecOps practices.
- [x] **Task 1.5 (Issue #5):** Scaffold initial GitHub Actions workflow for cross-compiling target triples (`x86_64-unknown-linux-gnu`, `aarch64-apple-darwin`, `x86_64-pc-windows-msvc`).
- [x] **Task 1.6 (Issue #7):** Refine the release process in `GEMINI.md` to mandate Semantic Versioning and annotated Git tags.
- [x] **Task 1.7 (Issue #86):** Documentation: Add Mermaid Visualizations for architecture and code path decision trees in `artifacts/` files. (HIGH PRIORITY)
- [x] **Task 1.8 (Issue #87):** DevSecOps: Implement AFR, Granular Labeling, and Env Sanity Checks. (HIGH PRIORITY)
- [x] **Task 1.9 (Issue #88):** DevSecOps: Implement Automated DORA Metric Generator and \`docs/DORA.md\`. (HIGH PRIORITY)
- [x] **Task 1.11 (Issue #112):** Advocacy/Debt: Create \`docs/CONFIGURATION.md\` as the centralized discovery point for all environment variables and their system impact. Update marketing site to expose this reference.
- [x] **Task 1.12 (Issue #150):** DevSecOps: Integrate Gitleaks into `scripts/pre-flight.sh`. (HIGH PRIORITY)
- [x] **Task 1.13 (Issue #151):** DevSecOps: Integrate Cargo-Audit into `scripts/pre-flight.sh`. (HIGH PRIORITY)

## Phase 2: Core Server (`pharos`) MVP
- [x] **Task 2.1 (Issue #6):** Implement core TCP listener and base RFC 2378 (Ph) syntax parser.
- [x] **Task 2.2 (Issue #8):** Implement the in-memory storage engine (Development Tier).
- [x] **Task 2.3 (Issue #9):** Implement the "Discriminator" logic to route requests as either `people` or `machine` records.
- [x] **Task 2.4 (Issue #10):** Implement standard application metrics (Push/Pull) and the "Health Monitor" threshold warnings.

## Phase 3: CLI Clients MVP
- [x] **Task 3.1 (Issue #11):** Implement read-only `ph` CLI client with basic query formatting.
- [x] **Task 3.2 (Issue #12):** Implement read-only `mdb` CLI client with basic query formatting.

## Phase 4: Advanced Storage & Authentication
- [x] **Task 4.1 (Issue #13):** Implement file-level, restart-survivable storage engine (Home Lab Tier).
- [x] **Task 4.2 (Issue #14):** Implement LDAP-backed storage engine and standard schema (Enterprise Tier).
- [x] **Task 4.3 (Issue #15):** Implement SSH-key-based authentication for Write operations on the server.
- [x] **Task 4.4 (Issue #16):** Update `ph` and `mdb` CLIs to support authenticated write/update commands.

## Phase 5: Release & Documentation
- [x] **Task 5.1 (Issue #17):** Finalize AGPL-3.0 License enforcement and headers in CI/CD.
- [x] **Task 5.2 (Issue #18):** Generate high-quality architecture diagrams and "How-To" guides.
- [x] **Task 5.3 (Issue #19):** Prepare v1.0.0 release with annotated Git tags and GitHub Release.

## Bug Tracker & Unplanned Work
*(Log any bugs discovered during feature development here. Do not fix them until the current task is complete. Ensure each gets a GitHub issue created via `gh`).*

**Verification Protocol (Strict Requirement):**
- **Issue Creation:** Include `**Failure Specifics**`, `**Proposed Fix**`, and `**Verification Strategy**`.
- **Issue Closure:** Include an **AI-Ready Verification Prompt** (Podman command) and a human-readable **Fix Summary**.

- [x] **Bug #81:** **Port Inconsistency**. `pharos-server` (2378) vs `pharos-console-web` (1050). (Remediation: Centralized in .env.example).
- [x] **Bug #82 (Issue #82):** **E2E Backend Failure**. `pre-flight.sh` fails to verify the TCP backend during Playwright runs. (Remediation: Spawn ephemeral backend in script).
- [x] **Bug #93 (Issue #93):** **Sandbox Key Access**. \`pharos-pulse\` cannot sign challenges due to missing keys. (Remediation: Shared volume + key discovery).
- [x] **Debt #01 (Issue #83):** **Auth Bridge Gap**. Web Console now supports automated SSH-challenge signing.
- [x] **Bug #114 (Issue #113):** **Login failure after first password change**. Secure cookies were being incorrectly dropped in HTTPS sandbox environments. (Remediation: Dynamic secure flag and SameSite=Lax).
- [x] **Bug #115 (Issue #114):** **Logout broken after mandatory password change**. Hover cursor is not a pointer and clicking logout does nothing. (Remediation: astro:page-load listener and cursor-pointer).
- [x] **Bug #116 (Issue #116):** **Pulse SIGTERM Failure**. \`pharos-pulse\` fails to respond to SIGTERM during startup or heartbeat, causing Podman to SIGKILL after 10s. (Remediation: Move signal handlers to start of \`main\` and select during \`wait_for_server\`).
- [x] **Debt #05 (Issue #115):** **GitHub Actions Modernization**. Update workflows to support Node.js 24 and remove deprecation warnings. (HIGH PRIORITY)
- [x] **Bug #126 (Issue #126):** **Pulse Baseline Presence Failure**. \`pharos-pulse\` fails to send baseline presence due to missing private key in Sandbox. (Remediation: Standardized paths to /etc/pharos/keys and improved client discovery loop).
- [x] **Bug #127 (Issue #127):** **Sandbox Terminal Query Failure**. Query \`hostname=pharos-main\` returns no result on home page terminal, but works in Search MDB. (Remediation: Standardized client ID to web-sandbox-mdb).
- [x] **Bug #139 (Issue #139):** **Advocacy: Marketing Simulation Drift**. Simulated UI components in `website/src/pages/index.astro` were out of sync with actual service logic/output. (Remediation: Updated all simulations to match real service output and design).
- [x] **Debt #06 (Issue #147):** **Upgrade protobuf to >= 3.7.2**. Mitigate uncontrolled recursion DoS (RUSTSEC-2024-0437).
- [x] **Bug #148 (Issue #148):** **Mitigate RSA Marvin Attack in ssh-key**. Side-channel timing attack for key recovery (RUSTSEC-2023-0071). (Remediation: Transitioned to Ed25519-only authentication and restricted ssh-key crate features).
- [x] **Bug #150 (Issue #152):** **Sandbox certgen fails with 'not found' error**. Resolved by switching to \`#!/bin/sh\` for better Alpine compatibility and ensuring explicit \`sh\` invocation in \`sandbox.yml\`. (Remediation: Added explicit \`depends_on\` chains to eliminate race conditions).
- [x] **Debt #07 (Issue #149):** Replace unmaintained fxhash dependency. Transition to ahash or rustc-hash for long-term support.
- [x] **Bug #160 (Issue #160):** **GitHub Action DORA Pulse failure**. Error in `scripts/generate-dora.sh` causing `sed` and `test` failures. (HIGH PRIORITY) (COMPLETED)


- [x] **Debt #02 (Issue #84):** **Logic Duplication**. Move all authorization logic from `lib.rs` into `SecurityTierMiddleware`.
- [x] **Debt #03 (Issue #85):** **Hardcoded Paths**. Transitioned SSH key paths to relative `PHAROS_KEYS_DIR`.
- [x] **Bug #103 (Issue #103):** **MDB Search Redirect**. `/mdb` incorrectly redirects to `/login` despite Guest search support. (Remediation: Updated middleware publicRoutes and session-to-locals flow).
- [x] **Task 105 (Issue #105):** **Filter 'unknown' inventory fields**. `pharos-pulse` sends 'unknown' for missing hardware data, cluttering mdb output. (Remediation: Omit fields with 'unknown' value in `collect_inventory`).
- [x] **Bug #107 (Issue #107):** **Sandbox certgen script permission denied**. The `pharos-certgen` container fails on startup with a 'Permission denied' error because `scripts/gen-sandbox-certs.sh` is missing the executable bit (+x).
- [ ] **Task 17.6:** Engineering: Implement `/health` API on Warp metrics server.
- [x] **Task 17.11 (Issue #108):** Engineering: Implement HTTP-to-HTTPS redirect on port 3000. Redirect plain HTTP traffic to HTTPS on the same listener.
- [x] **Task 17.12 (Issue #109):** Engineering: Include `pharos-scan` in the Pharos Toolbelt (bundled in Pulse image).
- [ ] **Task 17.7:** Advocacy: Document end-user decision paths in `docs/DECISIONS.md`.
- [x] **Task 6.1 (Issue #20):** CI: Verify standardized headers fails on Windows runner (bash syntax in pwsh).
- [x] **Task 6.2 (Issue #21):** Build: `pharos-server` compilation failure due to dependencies and type inference.
- [x] **Task 6.6 (Issue #32):** Deploy: GitHub Actions website deployment fails due to environment branch policy.
- [x] **Task 6.7 (Issue #36):** Bug: Mermaid diagrams not rendering on Architecture documentation page.
- [x] **Task 6.8 (Issue #77):** Bug: Container Image Publishing to GHCR fails with 'unknown blob' for `pharos-scan`.
- [x] **Task 6.9 (Issue #78):** Bug: `pharos-server` fails with 'GLIBC_2.39' not found in sandbox environment.
- [x] **Task 6.10 (Issue #79):** Bug: `pharos-server` default port (1050) mismatch with sandbox (2378).
- [x] **Task 6.11 (Issue #80):** Bug: `pharos-web` login fails due to CSRF origin check and secure cookie mismatch on http://localhost.

## Phase 6: Marketing & Open Source Advocacy
- [x] **Task 6.3 (Issue #24):** Advocacy: Scaffold GitHub Pages site with Sierra-inspired UX (iamrichardd.com/pharos).
- [x] **Task 6.4 (Issue #25):** Advocacy: Create high-quality architecture diagrams and "How-To" guides for Home Labbers and Enterprise Engineers.
- [x] **Task 6.5 (Issue #26):** Advocacy: Integrate DORA Metrics & Project Velocity Stats into the marketing site.

## Phase 7: Personal Website Integration (iamrichardd.com)
- [x] **Task 7.1 (Issue #27):** Advocacy: Integrate Pharos Landing Page into iamrichardd.com using existing Astro design system.
- [x] **Task 7.2 (Issue #28):** Advocacy: Port Pharos technical documentation to Astro MDX with Shiki highlighting.
- [x] **Task 7.3 (Issue #29):** Advocacy: Implement visual DORA Metrics & Project Velocity dashboard on the marketing site.
- [x] **Task 7.4 (Issue #30):** Advocacy: Align Pharos marketing site UX/UI with iamrichardd.com personal branding.

## Phase 8: Community & Ecosystem Expansion
- [x] **Task 8.1 (Issue #31):** Advocacy: Create a comprehensive `CONTRIBUTING.md` guide and project governance model.
- [x] **Task 8.2 (Issue #33):** Engineering: Implement a middleware/hook system in `pharos-server` for custom request processing.
- [x] **Task 8.3 (Issue #34):** Advocacy: Expand the marketing site with a community "Showcase" and integration guide.
- [x] **Task 8.4 (Issue #35):** Release: Prepare v1.1.0 release with the new hook system and community features.

## Phase 9: Documentation & UX Refinement
- [x] **Task 9.1 (Issue #37):** Advocacy: Refine documentation headers to remove "Pharos" prefix across all MDX pages.
- [x] **Task 9.2 (Issue #38):** Advocacy: Implement a tiered tabbed interface (Home Lab vs. Enterprise) in `howto.mdx` to reduce cognitive load.

## Phase 10: Network Discovery & Automation
- [x] **Task 10.1 (Issue #39):** Engineering: Refactor client logic into a shared `pharos-client` library with async support and SSH-auth.
- [x] **Task 10.2 (Issue #40):** Engineering: Implement the `pharos-scan` engine (mDNS, ARP, and Port Fingerprinting).
- [x] **Task 10.3 (Issue #41):** Engineering: Implement the interactive TUI and provisioning workflow for `pharos-scan`.
- [x] **Task 10.4 (Issue #42):** Release: Prepare v1.2.0 release including the new network scanner tool.
- [x] **Task 10.5 (Issue #110):** Engineering: Implement Subnet Scanning in `pharos-scan`. Support CIDR range discovery (Ping sweep/TCP probe).

## Phase 11: Marketing & Advocacy (v1.2.0)
- [x] **Task 11.1 (Issue #43):** Advocacy: Update marketing site with user-centric success messaging and the pharos-scan automated discovery feature.

## Phase 12: Advanced Visualization & Management (TUI)
- [x] **Task 12.1 (Issue #44):** Planning: Design a real-time TUI dashboard for \`pharos-server\` using \`ratatui\`.
- [x] **Task 12.2 (Issue #45):** Engineering: Implement the core dashboard logic and event stream.
- [x] **Task 12.3 (Issue #46):** Engineering: Integrate live \`pharos-scan\` events into the dashboard view.

## Phase 13: Future Expansion

## Phase 14: Pulse (Presence) & Multi-Tenant Architecture
- [x] **Task 14.1 (Issue #47):** Engineering: Implement `pharos-pulse` presence agent in Rust for cross-platform system manager integration (Systemd, SCM, launchd). Focused on Online/Offline status reporting.
- [x] **Task 14.11 (Issue #99):** Engineering: Refactor `pharos-pulse` to implement the "Baseline vs. Delta" inventory strategy. Collect hardware metadata (CPU cores/brand, serial, RAM) once at `ONLINE` and send minimal `HEARTBEAT` every 60 minutes.
- [ ] **Task 14.8 (#TBD):** Engineering: Implement "Presence Monitor" background sweep in `pharos-server` to infer `UNREACHABLE` state (70m timeout).
- [ ] **Task 14.9 (#TBD):** Engineering: Implement "Presence Fencing" logic in storage layer to prevent destructive automation on `UNREACHABLE` nodes.
- [x] **Task 14.12 (Issue #140):** Engineering: Implement `created_at` and `last_seen_at` field injection in `pharos-server` storage layer.
- [x] **Task 14.2 (Issue #49):** Engineering: Extend `pharos-server` security model to support Triple-Tier Security (open, protected, scoped) and Provenance Metadata.
- [x] **Task 14.3 (Issue #51):** Engineering: Develop the `pharos-console` backend (MCP Server) for AI-agent management.
- [x] **Task 14.4 (Issue #53):** Advocacy: Create multi-tenant documentation and `pharos-pulse` installation guides.
- [x] **Task 14.5 (Issue #55):** Advocacy: Apply tiered tab UX (Home Lab vs. Enterprise) to `integrations.mdx`, `architecture.mdx`, and `showcase.mdx`.
- [x] **Task 14.6 (Issue #56):** Advocacy: Perform Cognitive UX Audit and high-quality CLI animations.
- [x] **Task 14.7 (Issue #57):** Advocacy: Rearrange documentation information architecture to be tool-centric.

## Phase 15: Global Resilience & Advanced Connectivity
- [x] **Task 15.1 (Issue #59):** Engineering: Implement Multi-Server Synchronization for high-availability (HA) clusters using background replication.
- [x] **Task 15.2 (Issue #60):** Engineering: Implement Webhook Notification Engine for real-time Slack, Discord, and Custom API alerts on record modifications.
- [x] **Task 15.3 (Issue #61):** Engineering: Implement Advanced Pulse Alerting with configurable "Dead Man's Switch" logic for node failures.
- [x] **Task 15.4 (Issue #62):** Engineering: Sync marketing site theme with browser settings (`prefers-color-scheme`).

## Phase 16: Pharos Web Console (Human/AI/Mobile) - HIGH PRIORITY
- [x] **Task 16.1 (Issue #63):** Engineering: Scaffold the "Pharos Web Console" using Astro (SSR), explicitly separate from the static documentation site.
- [x] **Task 16.2 (Issue #68):** Engineering: Implement the "Web MDB" searchable interface for machine/infrastructure records (Mobile-First).
- [x] **Task 16.3 (Issue #65):** Engineering: Implement "One-off Addition" forms using Astro Actions with HitL confirmation.
- [x] **Task 16.6 (Issue #69):** Engineering: Align `pharos-console-web` with TDD, VSA, and SOLID mandates. Initialized Vitest, refactored to Vertical Slice Architecture, and implemented security reviews.
- [x] **Task 16.4 (Issue #66):** Engineering: Implement "Presence Monitoring" UI and Secure Logon (JWT-based).
- [x] Task 16.9 (Issue #90): Engineering: Implement CLI Handshake Authentication for Web Console.
- [x] Task 16.10 (Issue #91): Engineering: Implement MDB Handshake support and standardize CLI Handshake command in Web Console.
- [x] Task 16.8 (Issue #70): Advocacy: Refine marketing site for end-user focus (MDB & Automation). Move technical Zero-Host details to README.md.
- [x] **Task 16.5 (Issue #67):** Advocacy: Document the Human/AI/Mobile Web Console as the primary interface for non-technical users and mobile Home Labbers.
- [x] **Task 16.11 (Issue #98):** Engineering: Implement the high-density green bar chart visualization and pagination logic for MDB results. Handled 501 'No matches' normalization for consistent UI.
- [x] **Task 16.12 (Issue #100):** Engineering: Implement "Snake-Case Harmonization" for the Pharos Web Console to ensure 100% protocol fidelity. Refactored MachineStatus and UI components to use snake_case properties.
- [x] **Task 16.13 (Issue #101):** Engineering: Refactor MDB Search UI to focus on Inventory vs. Metrics. Removed visual bar chart and renamed header to 'Search Results' for improved clarity.
- [x] **Task 16.14 (Issue #102):** Engineering: Implement Unified Auth Gateway for the Web Console. Consolidate Username/Password, CLI Handshake, and Enterprise SSO into a single tabbed interface to reduce vertical clutter and cognitive load.
- [x] **Task 16.15 (Issue #113):** Engineering: Implement Mandatory Password Rotation on First Login. Create a secure, file-based `password-store.ts` using `scrypt` hashing and enforce updates for default credentials. (Issue #113)
- [x] **Task 16.16 (Issue #118):** Engineering: Refine Web Console UI (Remove Pulse, Streamline Session Info, Relocate Quick Add, Remove Help). (Issue #118)
- [x] **Task 16.17 (Issue #121):** Engineering: Consolidate MDB Search entry point. (Issue #121)
- [x] **Task 16.18 (Issue #142):** Engineering: Implement Browser-Based Date Formatting in Web Console. (Issue #142)

## Phase 17: Pharos Sandbox & WebMCP Integration
- [x] **Task 17.1 (Issue #71):** Engineering: Automate Container Image Publishing to GHCR. Created production `Containerfile`s and updated `ci.yml`.
- [x] **Task 17.2 (Issue #73):** Engineering: Design `deploy/sandbox.yml` with `tmpfs` and network isolation for "Zero-Host" test environments.
- [x] **Task 17.3 (Issue #72):** Advocacy: Create the "60-Second Success" Guide (The "One-Liner" Deployment).
- [x] **Task 17.4 (Issue #74):** Advocacy: Update `pharos-web` to support "Sandbox Mode" with live interactive query components and MCP integration.
- [x] Task 17.5 (Issue #75): Release: Prepare v1.3.0 release including the integrated Pharos Sandbox.
- [x] Task 17.8 (Issue #92): Engineering: Implement the 'Pharos Toolbelt' (bundle ph/mdb in Pulse) and the 'Sandbox-Aware' CLI Handshake UI in the Web Console. (Display 'podman exec' when PHAROS_SANDBOX=true).
- [x] Task 17.9 (Issue #104): Engineering: Pre-configure PHAROS_HOST=pharos-server in the Sandbox Pulse container for improved CLI DX.
- [x] Task 17.10 (Issue #128): Advocacy: Improve Sandbox Terminal placeholder text to emphasize MDB status queries.
- [x] Task 17.11 (Issue #129): Engineering: Format Sandbox Terminal output to match `mdb` CLI style.

## Phase 18: Enterprise Workflows (Alternation & Coalescing) - PROPOSED
- [ ] **Task 18.1 (#TBD):** Engineering: Implement Choice-Based Selection `[f1|f2]=val` (OR search) in `protocol.rs`.
- [ ] **Task 18.2 (#TBD):** Engineering: Implement Return Coalescing `return [f1|f2]` (First-match) in `pharos-server`.
- [ ] **Task 18.3 (#TBD):** Engineering: Implement `mapping.yaml` global alias support.

## Phase 19: Protocol Standardization (IETF Draft) - PROPOSED
- [ ] **Task 19.1 (#TBD):** Advocacy: Draft the "Pharos Protocol Extensions (PhP)" in IETF xml2rfc format.
- [ ] **Task 19.2 (#TBD):** Advocacy: Publish the PEPh (Pharos-Enhanced Ph) specification on the marketing site.
- [ ] **Task 19.3 (#TBD):** Release: Submit the Internet-Draft for Informational RFC consideration.

## Phase 20: Pharos Sandbox "Simulator Mode"
- [x] **Task 20.1 (Issue #94):** Engineering: Implement stateful, server-side challenge generation in `pharos-server` with a 5-minute TTL.
- [x] **Task 20.2 (Issue #95):** Engineering: Implement "First-to-Claim" identity bonding in storage layer and AuthManager.
- [x] **Task 20.3 (Issue #96):** Engineering: Update `pharos-pulse` with a resilient retry loop (exponential backoff) for server connectivity.
- [x] **Bug #117 (Issue #117):** Engineering: Resolve login lockout regression after password change. Fixed middleware redirect loop for actions and event listener loss during View Transitions.
- [x] **Bug #119 (Issue #119):** Engineering: Handle TLS `unexpected-eof` gracefully in `pharos-server`.
- [x] **Bug #120 (Issue #120):** Engineering: Resolve memory usage health alert threshold regression in `pharos-server`.
- [x] **Bug #122 (Issue #122):** Engineering: Resolve `ECONNREFUSED` and TLS `BadSignature` in Sandbox.
- [x] **Bug #123 (Issue #123):** Engineering: Resolve `ECONNREFUSED` in `mdb` CLI (ignores PHAROS_HOST).
- [x] **Bug #124 (Issue #124):** Engineering: Resolve Authentication Required error when adding records in Web Console (Persistent).
- [x] **Bug #128 (Issue #130):** Engineering: Resolve \"Signing failed: DECODER routines::unsupported\" in HitL Record Addition.
- [x] **Bug #134 (Issue #134):** Security: Secret Leak: Hardcoded OpenSSH Private Key in \`pharos.test.ts\`. (URGENT)
- [x] **Task 20.4 (Issue #97):** Engineering: Update \`deploy/sandbox.yml\` and \`Containerfile\`s for bootstrapped \`pharos-main\` registration.
- [x] **Task 20.5 (Issue #125):** Engineering: Implement \`deploy/sandbox.local.yml\` for Live HitL verification using on-host source code.

## Phase 21: Installation UX & Toolbelt Refinement
- [x] **Task 21.1 (Issue #131):** Planning: Design a frictionless installation experience (One-Liner & Unified Docs).
- [x] **Task 21.2 (Issue #132):** Engineering: Implement the \`scripts/install.sh\` automated installer.
- [x] **Task 21.3 (Issue #132):** Advocacy: Create \`website/src/content/docs/install.mdx\` and update marketing site.
- [x] **Task 21.4 (Issue #133):** Engineering: Implement \"Add Node\" UI and Contextual Search Guidance (examples/CLI links) in the Web Console.

## Phase 22: WebMCP Strategic Realignment
- [x] **Task 22.1 (Issue #135):** Engineering: Implement the \`/mcp\` JSON-RPC 2.0 Gateway in the Web Console. (HIGH PRIORITY)
- [x] **Task 22.2 (Issue #136):** Engineering: Implement Resource-First Realignment & Metadata Injection (Replace Fake Terminal).
- [x] **Task 22.3 (Issue #137):** Advocacy: Strategic Realignment of Marketing & Docs (v1.3.0 Agent-Native Vision).
- [x] **Bug #138 (Task 22.4):** Engineering: Normalize Ecosystem Property Names (os vs os_name). (Issue #138)
- [x] **Task 22.4 (Issue #141):** Engineering: Implement Human-Readable Flags for MDB CLI.
- [x] **Task 23.1 (Issue #143):** Advocacy: Transition to "Agent-Native Infrastructure" Standard. (HIGH PRIORITY)
- [x] **Bug #144:** Engineering: Untrack and Ignore Transient Test Results. (Issue #144)


## Phase 24: Marketing Engagement & Analytics
- [x] **Task 24.1 (Issue #145):** Planning: Engagement Measurement Strategy (Page Reads & Clicks).
- [x] **Task 24.2 (Issue #146):** Engineering: Implement Umami Cloud Analytics Integration. Integrated privacy-first tracking into `BaseLayout.astro`, `SandboxSnippet.astro` (copy-sandbox), and `TieredTabs.astro` (view-homelab/enterprise). Verified with a full pre-flight run.

## Phase 25: Fail Fast Architectural Debt
- [x] **Debt #08 (Issue #153):** Action: Require `ADMIN_PASSWORD` in production instead of silent default fallback.
- [x] **Debt #09 (Issue #154):** Action: Validate `PHAROS_PORT` with an `isNaN` check to prevent `NaN` propagation.
- [x] **Debt #10 (Issue #155):** Action: Return `ProtocolError::InvalidArgument` for unsupported wildcards instead of silent exact match fallback.
- [x] **Debt #11 (Issue #156):** Action: Log `error!` level messages for file system failures during key loading in `auth.rs`.
- [x] **Debt #12 (Issue #157):** Action: Refactor deeply nested `match` statements in `mdb/src/main.rs` using the `?` operator.
- [x] **Debt #13 (Issue #158):** Action: Implement MCP error mapping to distinguish between Parse, Invalid Request, and Internal Errors.
- [x] **Bug #159 (Issue #159):** Action: Allow unauthenticated read access for WebMCP gateway.
- [x] **Debt #14 (Issue #161):** Action: Redact `Auth`/`AuthCheck` signature and challenge material from `LoggingMiddleware`'s INFO-level request logging.
- [x] **Debt #15 (Issue #162):** Action: Detect and warn on TLS SAN/IP drift in `setup_pki()`'s existing-cert skip path instead of silently reusing a stale certificate.
- [x] **Debt #16 (Issue #163):** Action: Wire `PHAROS_PORT` into `pharos-server`'s actual bind address, or correct docs stating it controls the listening port (only `PHAROS_ADDR` does).
- [x] **Debt #17 (Issue #164):** Action: Fix `DECISIONS.md`'s security-boundary diagram to show `Status`/`Id`/`Login`/`Auth`/`Quit` bypassing the tier auth check in every tier.
- [x] **Debt #18 (Issue #165):** Action: Give `install_server`/`hub` a way to add an operator-supplied FQDN to `setup_pki()`'s certificate SAN — no path exists today, so any client connecting by DNS name (not bare IP or `localhost`) fails TLS hostname verification unconditionally.
- [x] **Debt #19 (Issue #166):** Action: Extend `pharos-server`'s SIGHUP-reload pattern to also re-read `PHAROS_TLS_CERT`/`PHAROS_TLS_KEY`, so integrators using an externally-managed renewing certificate (e.g. Let's Encrypt) have a restart-free path to pick up renewals.
- [x] **Debt #20 (Issue #167):** Action: Implement the RFC 2378 `change` command server-side — parsed since day one, never wired into `lib.rs`'s dispatch, silently returning "not yet implemented" for every real request.
- [x] **Debt #21 (Issue #168):** Action: Fix `automation.mdx`'s Proxmox pre-stop hook, which omits RFC 2378's required `make` clause and silently changes nothing even with `change` fully implemented.
- [x] **Debt #22 (Issue #169):** Action: Install a default rustls CryptoProvider in `pharos-client::connect()` — a v1.4.0 regression from Issue #61's `reqwest` addition left every client binary (`mdb`/`ph`/`pharos-pulse`/`pharos-scan`) panicking on every TLS connection, caught only by live-testing the actual published release binaries.
- [x] **Debt #23 (Issue #170):** Action: Webhook notifications (Issue #60) fire once per node in a peer-replicated cluster — the `forwarded` guard that stops `Command::Add` from re-replicating a peer-forwarded write doesn't also stop it (or `Change`/`Delete`, which have no `forwarded` marker at all) from re-notifying. Fixed with a uniform `SYNC ` wire-level forwarded marker for all three write commands, which also fixed a more severe pre-existing bug found while grounding this fix: `Change` had no forwarded-guard on replication at all, causing unbounded replication ping-pong between peers whenever a change's selection field didn't overlap its modified field (confirmed live before the fix).
- [x] **Debt #24 (Issue #171):** Action: The `SYNC ` forwarded-marker introduced by Debt #23/Issue #170 is unauthenticated — any client with write access can spoof it to suppress both replication and their own webhook notification, bypassing the audit trail. Needs real peer authentication (mTLS or fingerprint-allowlist) to close properly. Fixed via fingerprint-allowlist (panel-reviewed choice over mTLS, given zero PKI infrastructure exists and no production deployment to break): a new `peer` role, granted by the same filename convention as `admin`, gates whether the `SYNC` prefix's suppression effects are honored. A non-peer's spoofed prefix is now a no-op (write still executes, still replicates, still notifies) and logs a warning; a real peer's replicated writes are unaffected. Requires each node's own key to be registered with `peer` in the filename on every other node's `PHAROS_KEYS_DIR` — documented in server-setup.mdx.
- [ ] **Debt #25 (Issue #172):** Action: [DO NOT IMPLEMENT] Consider redesigning Issue #59's multi-master mesh replication as a primary/secondary (leader-follower) topology instead, to structurally eliminate the whole class of ping-pong/spoofing problems in Debt #23/#24 — a bigger design question, not a quick patch.
- [x] **Debt #26 (Issue #173):** Action: `pharos-scan` has no non-interactive/scripted output mode — `network-scan.mdx` documented a `--json`/`--filter` mode that never existed in the CLI (removed from docs; implement for real or leave unimplemented). Fixed: added a real `--json` flag (via `clap`, matching `ph`/`mdb`'s existing convention) that skips the interactive TUI and prints fully-enriched discovered nodes as a JSON array to stdout, with all logging routed to stderr so the output is safe to pipe to `jq` — deliberately no `--filter` DSL, per the issue's own recommendation. Incidentally fixed a real, pre-existing bug found along the way: `cargo build -p pharos-scan` in isolation failed even on unmodified `main` due to a feature-unification gap with `pharos-client`'s `tokio-rustls` usage; added the missing feature directly to `pharos-scan`'s own manifest.
- [x] **Debt #27 (Issue #174):** Action: `install.sh` mis-serves two real platforms — no `linux-aarch64` release asset exists despite being requested for detected ARM64 Linux hosts, and macOS Intel hosts are silently served the `macos-aarch64` binary regardless of detected arch (docs corrected to state actual current support; script/CI gap still open). Fixed with a reshaped release matrix (product decision, not just a bug fix): `linux-aarch64` now gets full support (all 5 binaries, native `ubuntu-24.04-arm` GitHub-hosted runner — no cross-compilation), while macOS is narrowed to `ph`/`mdb` only (client tools, since a laptop isn't where you'd run the server/scanner/pulse agent) and Intel Macs remain unsupported with a clear error rather than a silent wrong-arch download. `install.sh` now checks both arch and component-per-OS before attempting a download. Not fully verifiable pre-merge: the new ARM64 CI runner can only be proven out by an actual tagged release run.
- [x] **Debt #28 (Issue #175):** Action: RFC 2378 `set` session options (`limit`, `addonly`, etc.) are completely unimplemented — no server-side cap on how many records a single `change`/`delete` can affect. Fixed: `Command::Set` now dispatches for real, backed by a per-connection `SessionOptions` (all 7 RFC options settable/reportable); `limit` and `addonly` are enforced with real safety semantics (RFC errors 518/521) via a pre-flight `query()` check taken under the same write-lock as the mutation (no TOCTOU race), skipped entirely when no limits are configured so the default (unlimited) path pays no extra cost. `echo`/`charset`/`verbose`/`nolog`/`external` are accepted/reported but intentionally inert (no field-keyword/ACL system exists to enforce `external`; the others have no user-visible effect to implement against) — tracked as a documented deviation, not a gap, per Debt #31/Issue #178.
- [x] **Debt #29 (Issue #176):** Action: Response status codes conflict with RFC 2378's defined meanings in several places (403/401 used inconsistently for "not authorized"/"not logged in"; 598/599 each reused for two different conditions; 500 used generically where RFC's 517 fits the read-only case). Fixed: standardized on RFC's own codes throughout — `506` for "not logged in" (was `401`, which RFC defines as "Internal database error"), `510`/`511`/`516` for change/add/generic authorization denial (was generic `403`, which RFC defines as "login OK but database read-only"), `517` for the read-only-storage case (was generic `500`), `512` for illegal selection values (was the non-RFC `421`), `513` for an unrecognized `set` option (was `512`, now reserved for a recognized option with a bad value). `598`/`599` are now reserved exclusively for their RFC meanings ("Command unknown" / "Syntax error"); the "recognized command, no dispatch logic yet" case moved to a new, documented Pharos extension code `597` so it's no longer indistinguishable from a real unknown command.
- [x] **Debt #30 (Issue #177):** Action: Implement RFC 2378's `fields` (schema discovery) command — parsed since day one, never dispatched, the main interop gap for any non-Pharos Ph client. Fixed: `Command::Fields` now dispatches, reporting a code-grounded baseline (`type`, `hostname`, `alias`, `created_at`, `last_seen_at`, `status`) merged with whatever fields are actually harvested from stored records, each with a stable alphabetically-assigned id, RFC's two-line-per-field format, and `507:Field does not exist` when none of the explicitly requested names exist. No RFC field keywords (`Indexed`/`Unique`/`Encrypt`) are fabricated — Pharos has no field-ACL system to back them (tracked as Debt #31/Issue #178). Note: schema discovery is necessarily global across teams today (reveals field *names*, never values) since there's no field-level ACL to scope it by — a known deviation, not a regression.
- [x] **Debt #31 (Issue #178):** Action: Document Pharos's known RFC 2378 compliance deviations (no field-level ACL/keyword system, RFC's native login mechanisms replaced by SSH-key auth, several parsed-but-undispatched commands) so they read as intentional, not as bugs. Fixed: added a "Known RFC 2378 Deviations" section to both `website/src/content/docs/architecture.mdx` (full detail) and `docs/ARCHITECTURE.md` (compact summary linking to the website), covering all six points current as of v1.7.0 — no field-level ACL (and its `fields`-command global-visibility consequence), SSH-key auth replacing RFC's native login mechanisms, the seven still-undispatched commands (`SiteInfo`/`Logout`/`Answer`/`Clear`/`Email`/`XLogin`/`Help`, now excluding `set`/`fields` since those shipped), `set`'s five inert options, the `597` extension code and 510/511/516 authorization-code judgment calls from Debt #29, and the one remaining wildcard gap (no escape mechanism for literal wildcard characters) from Debt #32.
- [x] **Debt #32 (Issue #179):** Action: Full RFC 2378 wildcard support (`+`, `?`, `[abc]`, non-suffix `*`) — Issue #155/Debt #10 fixed the failure mode (loud error vs. silent wrong match) for unsupported wildcards, but only a single trailing `*` is actually supported. Fixed: `wildcard_match` replaced with a hand-rolled dynamic-programming glob matcher (polynomial time by construction, no ReDoS risk, no new dependency) supporting all four RFC forms in any position/combination; malformed patterns (unclosed/empty `[...]`, stray `]`) still fail loudly with `512:Illegal value`, per Issue #155's original principle. Known accepted limitation, unchanged by this fix: no escape mechanism exists, so a literal `*`/`+`/`?`/`[`/`]` can never appear in a search value — worth a line in Debt #31/Issue #178.
- [x] **Debt #33 (Issue #180):** Action: `install_web_console()`'s hub role primes `/etc/pharos/certs/pharos-web.crt`/`.key` via `setup_pki`, but `console.mdx`'s container Quick Start generates its own separate self-signed cert instead of consuming it — the primed cert is orphaned even when the docs are followed exactly. Reported by the Implementation team while standing up pharos-01: correctly traced port 2378's `Received HTTP/0.9` error back to `pharos-server` only speaking the raw control protocol (no web console listening anywhere), then found the unused cert pair. Not a regression — unchanged since `install_web_console` was introduced (2026-07-28, commit `c9efc53`), predating all tracked releases. Fixed: rather than consuming the orphaned `pharos-web.crt` (which nothing external ever refreshes, so it wouldn't help real-certificate/Let's-Encrypt deployments), removed that priming entirely and had the console reuse `pharos-server.crt`/`.key` directly — the file any cert-sync process and `pharos-server`'s own SIGHUP-reload already keep current. Also fixed a second, previously-unnoticed bug along the way: the old self-signed cert's SAN only covered `localhost`/`127.0.0.1`, while the docs told operators to access it via `<your-server-ip>` — following the docs exactly produced a cert that failed hostname verification at the one address it was meant for. Live-verified end-to-end: real cert generated via `setup_pki`, real `pharos-console-web` container run against it (fingerprint match), `sudo`/`pharos`-group requirement confirmed necessary, restart-required-for-renewal behavior confirmed.
- [x] **Debt #34 (Issue #181):** Action: `pharos-pulse` never provisions a signing key for itself — `install_pulse()` generates certs and configures the systemd unit but never creates/enrolls an identity for the pulse agent to authenticate as. Confirmed live: on a real node against a Protected-tier hub, pulse retried baseline registration every ~60-120s for 20+ minutes straight, every attempt failing with "No private key found for signing" (it runs as `User=pharos`, a system user with no home directory, so every path `sign_message_async` checks is empty). Presence registration silently never succeeds against any Protected/Scoped-tier hub. Fixed in v1.10.9 (`User=pharos` deliberately kept unchanged, per explicit direction): `install_pulse()` now generates a dedicated Ed25519 key at `${PHAROS_DIR}/keys/pulse_id_ed25519`, points `PHAROS_PRIVATE_KEY` at it explicitly in the systemd unit, and auto-enrolls it on the hub via the existing `--fetch-ca-ssh` SSH connection (falling back to a printed one-liner otherwise). Live-verified end-to-end against the actual published v1.10.9 release binaries (real `pharos-server`/`pharos-pulse`, real Protected-tier hub, real SSH enrollment): baseline presence registered successfully with zero auth errors.
- [x] **Debt #35 (Issue #182):** Action: `mdb`/`ph` don't support `--version` — neither CLI struct sets clap's `version` attribute. Fixed: added `#[command(version)]` to both (pulls `CARGO_PKG_VERSION` automatically, same mechanism already used for pharos-pulse's startup banner). Along the way, found and fixed a real gap in `scripts/bump-version.sh`: it only ever updated `crates/pharos-pulse/Cargo.toml`, never `mdb/Cargo.toml` or `ph/Cargo.toml` — both were stuck at the crate-template default `0.1.0` this whole time, meaning the new `--version` flag would have shipped permanently reporting the wrong version. Extended the script to cover both, matching its existing pattern exactly, and brought both `Cargo.toml`s up to the current version. Live-verified: `mdb --version`/`ph --version` correctly report the real current version; `cargo test --workspace` passes.
- [x] **Debt #36 (Issue #183):** Action: node install's "Next Steps" never explains that `ph`/`mdb` need `PHAROS_SERVER`/`PHAROS_HOST` exported in the operator's own shell to reach the hub — that env var only ever lives inside `pharos-pulse`'s systemd unit. Confirmed live: an operator hit `mdb *` defaulting to `127.0.0.1:2378`, then hit `VAR=value` without `export` looking like it worked (`echo` shows it) while remaining invisible to `mdb`. Fixed structurally rather than just documented: `install_pulse()` now persists the resolved hub host to `/etc/pharos/client.conf` (world-readable, `644`, host:port only — no secrets), and `mdb`/`ph` fall back to reading it via a new shared `pharos_cli_support::resolve_server_address()` (also consolidating what had been duplicated resolution logic between the two binaries). Precedence preserved: `PHAROS_SERVER` env var → `PHAROS_HOST`/`PHAROS_PORT` env vars → `/etc/pharos/client.conf` → hardcoded default. Panel-reviewed twice; connection-failure errors now also state which of the four sources the address was resolved from, so a stale/misconfigured host is diagnosable instead of just repeating the address back. Verified empirically that a non-root local user cannot tamper with `client.conf` (root:root, `755`/`644`, write denied). Live-verified end-to-end in a completely clean shell environment (`env -i`, zero `PHAROS_*` vars) against a real disposable hub.
- [x] **Debt #37 (Issue #184):** Action: `check_cert_ip_drift()` false-positives on certs with zero IP SANs at all (e.g. a real Let's Encrypt cert), permanently suggesting a "fix" (regenerate the cert) that would destroy a real certificate on every future `install.sh` run. Fixed in v1.10.9 per `artifacts/ip-drift-false-positive-plan.md`: an early return when a cert has zero IP SANs at all, since `setup_pki()` always includes at least `127.0.0.1` and a cert with none was never generated by this script. Verified against real openssl-generated certs: genuine drift (cert has IP SANs but is missing the current LAN IP) still warns correctly; the zero-SAN case is now silent.
- [x] **Debt #38 (Issue #185):** Action: `mdb`/`ph` should interactively offer to generate (and optionally enroll) a signing key on a missing-key error instead of only printing static instructions. Plan already written: `artifacts/interactive-key-setup-plan.md` — local generation gated on `std::io::IsTerminal` (safe for scripts/CI), remote enrollment reusing `--fetch-ca-ssh`'s proven opt-in/non-interactive-safe pattern. Fixed: implemented in `mdb`/`ph`'s own `main()` only (`crates/pharos-client` untouched). Panel-reviewed twice; findings addressed in a follow-up dedup pass (`artifacts/mdb-ph-key-setup-dedup-plan.md`) — the ~250 lines duplicated between `mdb`/`ph` were extracted into a new shared crate, `crates/pharos-cli-support` (depended on by `mdb`/`ph` only, never `pharos-pulse`); a DevSecOps finding (unsanitized `$USER` embedded in the remote SSH enrollment command) and a UX finding (enrollment offered on any retry failure, not just auth-related ones) were both fixed and live-verified. Live-verified end-to-end (real key generation, real SSH enrollment, real Protected-tier hub auth) for both binaries, plus TTY-gating, decline path, argument-injection rejection, and existing-wrong-type-key handling.
- [ ] **Debt #39 (Issue #186):** Action: `mdb`'s raw-command whitelist (`add`/`change`/`delete`/`status`/`siteinfo`/`quit`) silently wraps anything else, including RFC 2378's own `login` command, in `query <text>` before sending it — `mdb login richard` doesn't attempt a login, it searches for the literal string "login richard". Low real-world impact today (`execute_authenticated()` already handles login transparently for any normal command; manual `login` was never the intended flow), but a real, silent correctness gap for anyone invoking RFC 2378 commands manually.
- [ ] **Debt #40 (Issue #187):** Action: `auth_retry_integration.rs`'s `test_should_transparently_auth_retry_when_506_received` fails reproducibly under the full `cargo test --workspace --all-features` run (all 7 crates' test binaries concurrently) but passes in isolation and in every subset combination tried. Confirmed absent on clean `HEAD` (via an isolated `git worktree`) before `crates/pharos-cli-support` was added — the failing test and its full dependency chain (`pharos-server`, `pharos-client`) are byte-for-byte unchanged, so this isn't a regression in that work, just newly exposed by crossing some concurrency threshold. Ruled out container resource limits (`--pids-limit=-1 --ulimit nofile=65536:65536` didn't fix it) and naive parallelism limiting (`--jobs 1`, `--test-threads=1` didn't fix it either, since neither actually serializes cross-binary test execution). Needs proper root-cause: likely a pre-existing test-isolation gap (e.g. in `gen-sandbox-certs.sh`'s shebang/interpreter resolution under heavy concurrent `exec()`) that any future crate addition could equally have triggered.
- [x] **Debt #41 (Issue #188):** Action: `install_server()` chowns/chmods `certs/` and `keys/` correctly but never touches `PHAROS_DIR` (`/etc/pharos`) itself, leaving it `root:root 755` — `persist_to_disk_atomic()`'s temp-file-plus-rename pattern needs write access on the containing directory, not just `data.json`, so `pharos-server` (running as `User=pharos`) has never been able to persist to disk on any hub. Found live in production: failing silently since ~20:30 with `Failed to persist records to disk: Permission denied` logged every ~30 minutes, `data.json` never once created, discovered only while capturing a baseline for a disaster-recovery storage migration (see `iamrichardD/home_network#3`) — `add` commands return `Ok` to the client regardless since the failure happens asynchronously in the background persistence worker. Fixed: `install_server()` now chowns/chmods `PHAROS_DIR` itself (`pharos:pharos`, `700`), matching the exact convention already used for `keys/`. Independently reproduced both the bug (real `pharos-server` as `User=pharos` against the old `root:root 755` permissions produces the verbatim reported error) and the fix (clean persistence, real record confirmed on disk) via a live before/after comparison, not just static permission inspection. `certs/`/`keys/` permissions confirmed unaffected.
- [x] **Debt #42 (Issue #189):** Action: `mdb *`/`ph *` against zero matching records prints `Error: 501: No matches to query` and exits non-zero, making a normal empty-result outcome look like a failure. Confirmed server-side: status `501` is emitted identically for `query`/`change`/`delete` all finding zero matches — never a genuine error in any of the three cases. Fixed: `parse_response()` now special-cases `501` (mirroring the existing `506` special case) to return `PharosResponse::Matches { count: 0, records: vec![] }` instead of the generic `Error` bucket; `mdb`/`ph` print an explicit "No matches found." for the empty case and exit `0`. Live-verified: empty query/change/delete all report cleanly with exit `0`; a genuine error (malformed query, status `512`) still reports as an error with non-zero exit, confirming the reclassification didn't swallow real failures; non-empty match results confirmed completely unchanged.
- [x] **Debt #43 (Issue #190):** Action: `pharos-pulse`'s `collect_inventory()` stored `sys.total_memory()` directly into the `mem_total_kb` field with no unit conversion — `sysinfo` 0.30's `total_memory()` returns bytes, not KB, despite the field's name and every downstream consumer (`mdb`'s `format_human()`) treating it as genuine KB. Off by exactly 1024x, confirmed live in production (`proxmox-01`'s reported value was the real `free -k` total times exactly 1024) during an unrelated fleet-health verification pass. Fixed: extracted a pure `bytes_to_kb()` helper (needed since `collect_inventory()` itself reads real, non-mockable system state) and applied it to the field. Regression test hardcodes the actual production-observed value. Confirmed isolated to `pharos-pulse` — `pharos-scan` (which depends on a different `sysinfo` version) has no equivalent memory-reporting field. Self-healing on already-running hubs without a data migration, since pulse re-sends this field on every heartbeat.
- [ ] **Debt #44 (Issue #191):** Action: consider a plausibility/sanity check for inventory fields (server-side ingestion or `format_human()`) so a *future* unit-mismatch bug (another `sysinfo` upgrade, a new inventory field) fails loudly instead of silently producing a wrong-but-plausible-looking number, the way Debt #43/Issue #190 did undetected until a human happened to check the raw numbers. Raised during that fix's panel review, deliberately not bundled into it — a real design question (where the check belongs, what counts as "implausible," scope), not a quick patch.
- [x] **Debt #45 (Issue #192):** Action: `mdb`/`ph` gave zero visibility into what actually happened for a given invocation — which server address was resolved (and from where), the exact wire command sent, or the raw response received. A clean "No matches found." looked identical whether a query genuinely matched nothing or silently reached the wrong server or (as found live, see Debt #46/Issue #193) hit unquoted shell glob expansion. Fixed in v1.10.14: added a `--debug` flag to both clients (off by default, zero output change otherwise) printing the resolved address+source, the exact wire command, and the raw `PharosResponse` to stderr. Verified against the actual downloaded v1.10.14 release binaries, not just a local build.
- [x] **Debt #46 (Issue #193):** Action: `mdb *`/`ph *` unquoted gets expanded by the shell into every filename in the current directory before the client ever sees it — a real information-disclosure risk, not just a UX nit. Confirmed live: an operator's `~/Downloads` listing (tax documents, recovery-code files, access-key CSVs) was silently sent to the hub and logged in plaintext, indefinitely, in `journalctl -u pharos-server`. Not documented anywhere either. Fixed in v1.10.14: a shared, non-blocking heuristic (`pharos_cli_support::warn_if_looks_like_glob_expansion`) warns to stderr when a command looks like it hit this (15+ bare tokens, none in `field=value` form) while still proceeding, so legitimate scripted usage isn't broken; `cli-clients.mdx` now has an explicit quoting-warning callout that didn't exist before.
- [x] **Debt #47 (Issue #194):** Action: a `Record`'s `record_type` (structured cache used for the mdb-vs-ph discriminator) and its raw `fields["type"]` (the RFC 2378 wire field) were two sources of truth with nothing keeping them in sync after creation. Confirmed live: a host's first registration attempt happened to land without a `type` field, permanently stuck with `record_type: None` — every later `pharos-pulse` heartbeat correctly resent `type=machine` into `fields`, but `record_type` never updated, making that host invisible to every `mdb` query forever with no indication anything was wrong. Fixed in v1.10.14 per `artifacts/record-type-integrity-plan.md` (panel-reviewed before implementation): `mdb`/`ph` `add` now always force the correct `type`; the server rejects `add` with no `type` field, rejects `upsert` attempting to change an existing record's `type` (comparing against the record's own `fields["type"]`, not the potentially-stale `record_type` cache — a panel correction), and rejects any `change` touching `type` at all; a synchronous startup self-heal (must complete before accepting connections — the panel's other required correction) recomputes `record_type` from `fields["type"]` for existing on-disk records. Live-verified twice: locally in Podman during implementation, and again against the actual downloaded v1.10.14 release binaries — reconstructed the exact broken state and confirmed the self-heal log line fires and the record becomes queryable end-to-end via the real `mdb` binary.
- [x] **Debt #48 (Issue #195):** Action: the Implementation Team found `pharos-console-web` running v1.10.11 while the rest of the fleet was on v1.10.14 — its systemd unit pins `:latest` but only pulls on an empty local image cache, so it silently never moved after a migration wiped that cache once, with nothing in Pharos itself able to notice. Fixed per `artifacts/console-version-self-report-plan.md` (panel-reviewed: extend the existing Dead Man's Switch rather than build a parallel Terraform-side checker): `pharos-console-web` now self-registers as a Machine record on startup and periodically (version sourced from the actual release tag via a build-arg, never `package.json`, which was already drifted/unrelated), displays `Pharos v<version>` in its own footer for human-visible confirmation, and `pharos-server/src/alerting.rs` gained `find_version_mismatches`/`check_version_mismatches` (mirroring `find_newly_stale`'s existing pattern) that fires the same webhook/script alert on a `version`/`expected_version` mismatch — alert-only, no auto-upgrade, no autonomous GitHub polling; `expected_version` is always operator/Terraform-set. Also bumped `pharos-console-web/Containerfile` off the stale `node:22-slim` base to `node:24-slim` (current LTS), independently verified after the first two implementation passes silently failed to apply this explicitly-requested change despite claiming full completion — rebuilt and live-verified end-to-end myself against a real disposable `pharos-server` and the actual rebuilt image (self-registration, footer render, and version-mismatch webhook all confirmed on `node:24.18.0`).
- [x] **Debt #49 (Issue #196):** Action: within hours of v1.10.15 reaching production, the Implementation Team's rollout hit two real gaps in Debt #48/Issue #195's feature. (1) `pharos-console-web` self-reports `version` with a `v` prefix (`v1.10.15`) but the image tag has none — `find_version_mismatches()`'s exact string comparison meant following the (wrong) example in the initial response doc would have created a permanent, silently-never-repeating false positive (the alert-dedup logic fires once for a mismatched pair and then never again, reading as "resolved" when it means "will never be reported again"). (2) `getConsoleHostname()`'s fallback to bare `os.hostname()` deterministically collides with the same host's `pharos-pulse`-owned record for any operator who already sets `--hostname` to match the physical host — not a rare misconfiguration, the expected outcome for that common practice. Fixed per `artifacts/console-version-hardening-plan.md` (panel-reviewed): a `normalize_version()` helper strips an optional leading `v`/`V` at comparison time only (never changes what's stored/displayed); the hostname fallback default changed to `${os.hostname()}-console`, avoiding the collision by construction while `PHAROS_CONSOLE_HOSTNAME` still fully overrides it. Both production incidents are now permanent regression tests (`pharos-server/tests/console_version_hardening_live.rs`). Live-verified: `cargo test --workspace` and the console's own `npm test` (on `node:24-slim`) both pass, including the two new live-verification tests reproducing the exact production scenarios.
- [x] **Debt #50 (Issue #197):** Action: Pharos had no structured IP/MAC address field support — mdb/ph accepted ip=/mac= as arbitrary free-form text, pharos-scan captured a single MAC internally never sent to the server, and there was no way to represent a device with multiple NICs or both an IPv4 and IPv6 address. Driving use case (confirmed directly with the reporting engineer, a former RFC 2378 implementer): a security-response service that locates a device by IP/MAC (supplied by virus-scanning software) to shut a switch interface or blackhole it — pure membership lookup, no need for IP-to-MAC-to-interface correlation. Initial research wrongly concluded RFC 2378 has no multi-valued-field mechanism; corrected after direct operator pushback — the response grammar's field name is optional per line (section 2.2), and a continuation line omitting it adds another value to the previous field, confirmed against a real implementation's output. Fixed per artifacts/multi-value-ip-mac-fields-plan.md (panel-reviewed), replacing the interface-suffixed ip4_<iface>/mac_<iface> convention shipped in v1.10.16 (which was designed under the wrong RFC assumption, see the superseded artifacts/multi-nic-ip-mac-fields-plan.md): Record gained a parallel multi_fields: HashMap<String, Vec<String>> scoped to ip_addr/mac_addr only (every existing fields consumer untouched), add/change append idempotently rather than replace, query matching checks list membership, the server emits the genuine RFC continuation-line wire format, pharos-pulse auto-populates real interface data via if-addrs (sysinfo 0.30 doesn't expose per-interface IPs) while correctly excluding any interface with no assigned IP (proven against real bonded/bridged Proxmox topology — physical NICs enslaved to a bond, the bond itself, and container veth pairs all excluded; only the bridge with the real address is emitted), and both fields get fail-closed format validation. Live-verified end-to-end against real binaries in a disposable Podman node: mdb's own output already renders the continuation-line format correctly with zero client-side changes needed, matching the real-world example that prompted the correction exactly.
- [x] **Debt #51 (Issue #198):** Action: `pharos-pulse`'s existing `serial_number` field has silently never worked in production — confirmed live via `mdb '*' return hostname serial_number` against the real fleet (10 hosts, including bare-metal ones), showing the field completely absent everywhere, not even as `unknown`. Root cause (Linux only): `/sys/class/dmi/id/product_serial` is root-restricted (0400) by design on most distros, a deliberate anti-fingerprinting measure, and pharos-pulse deliberately runs as an unprivileged `User=pharos` (preserved intentionally, see Debt #34/Issue #181). Separately, real-world asset-management value was identified (via a real `sudo dmidecode -t system` transcript) in also collecting uuid, manufacturer, and product_name — uuid shares serial_number's exact root cause; the latter two are not permission-restricted. Fixed per artifacts/dmi-hardware-fields-fix-plan.md (panel-reviewed): scripts/install.sh's install_pulse() installs a narrow systemd-tmpfiles.d drop-in granting the pharos user read-only ACL access to exactly product_serial and product_uuid, applied immediately at install time and re-applied automatically at every boot (sysfs is kernel-regenerated, so a one-time grant would otherwise be lost on reboot) — no dmidecode, no broad sudo, no capability grant wider than these two files. pharos-pulse gained uuid (same permission-gated mechanism) and manufacturer/product_name (not permission-restricted) across Linux/macOS/Windows, sharing a new read_sysfs_dmi_field() helper on Linux that also fixed a latent gap (an empty-but-present sysfs file now correctly reads as unknown, not just an outright read failure). Live-verified via the new scripts/live-verify/ Podman harness: a real mdb query returned manufacturer: Dell Inc. / product_name: XPS 15 9500 — genuine host DMI data read through the container's shared kernel sysfs, not mocked. Explicitly NOT claimed as verified via Podman: the actual systemd-tmpfiles ACL fix for serial_number/uuid, since containers don't expose an independent, permission-settable DMI sysfs — that step still needs confirmation on a real bare-metal or VM Linux host.
- [x] **Debt #52 (Issue #199):** Action: Debt #51/Issue #198's fix (v1.10.18) for pharos-pulse's broken serial_number field used systemd-tmpfiles's `a`-type (ACL) entries to grant the pharos user read access to /sys/class/dmi/id/product_serial and product_uuid. Confirmed live on real production hardware (rdelgadoXPS15) while verifying the fix actually took effect: it doesn't - sysfs does not support POSIX ACLs at all (`setfacl -m u:pharos:r /sys/class/dmi/id/product_serial` returns `Operation not supported`), and systemd-tmpfiles's ACL type silently no-ops on the same limitation, masked by the installer's own `|| true` (kept for the legitimate reason of tolerating hosts where the DMI paths don't exist at all, but it also hid this). This is exactly the class of bug the original plan flagged as impossible to verify via Podman - containers don't expose an independent, permission-settable DMI sysfs, so only real hardware testing could catch it. Fixed: switched to `z`-type (plain ownership/mode adjustment) tmpfiles.d entries, which sysfs does support (`z /sys/class/dmi/id/product_serial 0640 root pharos - -`). Confirmed end-to-end on real production hardware: after applying the corrected drop-in and restarting pharos-pulse, a real mdb query against the real hub returned `serial_number: 47Y0893` and `uuid: 4c4c4544-0037-5910-8030-b4c04f383933`, matching the device's actual DMI data exactly.
- [x] **Debt #53 (Issue #200):** Action: Debt #50/Issue #197's multi-valued ip_addr/mac_addr fields (Record.multi_fields) exposed a real production data-integrity gap. A record created before that feature existed (rdelgadoXPS15, created_at 2026-08-05) had ip_addr/mac_addr stored as plain single-value strings directly in `fields`, not `multi_fields`. The query-response rendering code checks `fields` before `multi_fields` for a given key name, so a record with the same key in both maps silently shows only the stale plain value forever, completely hiding the correctly-collected multi-value data right next to it - the same "two sources of truth for the same fact" class of bug as Debt #47/Issue #194 (record_type/fields["type"]), recurring for a different field pair. Confirmed via direct code review that all three write paths (add_record, upsert_record, change_record) already correctly route ip_addr/mac_addr into multi_fields - this was purely a migration gap for pre-existing records, not a live write-path bug. Fixed: added a self-heal migration step to FileStorage::load_from_disk(), mirroring the exact pattern already established for record_type - any record with a legacy plain-string ip_addr/mac_addr in `fields` has it removed and folded into multi_fields (deduplicated), logged, and persisted back to disk, synchronous, before the server accepts connections. Deployed to real production (pharos-01.iamrichardd.com) and confirmed end-to-end: self-heal log line fired on restart ("Migrated 2 legacy plain-string ip_addr/mac_addr fields into multi_fields"), and a fresh pharos-pulse registration from the real affected host now correctly shows the full multi-value list - all 5 real IPs and 4 real MACs, including the actual physical WiFi interface that had been completely invisible this whole time. Rest of the fleet (10 hosts) confirmed unaffected after the hub restart. New permanent regression test reproduces the exact production record shape.
- [x] **Debt #54 (Issue #201):** Action: Implementation Team reported multi-record full-output queries (`mdb -H '*'`, `mdb type=machine`) hanging forever, while single-record full output and multi-record single-field-projected output both worked fine. Root cause: `handle_connection` never called `.flush()` on the TLS write-half anywhere - `write_all()` only queues plaintext into rustls' internal buffer, and a response large enough to hit backpressure partway through (many records, each with many fields) left its tail silently unflushed once the loop moved on to await the next command, blocking the client forever reading bytes that were never actually sent. Confirmed live against real production data (pharos-01.iamrichardd.com's data.json, via `scripts/live-verify/`): server-side tracing showed the response loop completing and writing `200:Ok` every time, while the client consistently stalled at the identical byte offset regardless of timeout length - ruling out a slow response and confirming a genuine stuck write. Fixed: three `.flush()` calls in `handle_connection` covering every exit path uniformly (after the initial banner; at the top of the command loop, flushing the previous iteration's writes before blocking on the next read - covers the normal path plus both early `continue`s; after the loop exits, covering `Command::Quit`'s write just before its `break`) rather than sprinkling flushes after every individual `write_all()` across a dozen command arms. New permanent regression test (`pharos-server/tests/flush_on_large_query_response.rs`) spins up a real TLS-wrapped listener (self-signed cert via `rcgen`, no external `openssl` binary dependency, unlike `scripts/gen-sandbox-certs.sh`) seeded with 20-30 synthetic records shaped like real production inventory, and asserts a full `query *`/`query type=machine` response arrives within a bounded timeout - a plain non-TLS `TcpStream` (what every other integration test in this suite uses) can't reproduce this bug at all, since rustls' internal write-buffering is the actual mechanism being guarded against. Verified both directions: fails with a clear diagnostic message against the pre-fix code (reproducing the exact production hang), passes against the fix. Panel-reviewed twice (fix, then test). Live-verified against real production data in the compose harness: both previously-hanging queries now complete in under 3 seconds.
- [x] **Debt #55 (Issue #202):** Action: Implementation Team reported pharos-console-web's CLI Handshake login tab showing "Handshake verification failed" even after correctly running `mdb auth sign <challenge>` with an already-authenticated CLI identity and pasting the resulting public key/signature. Root cause: `SecurityTierMiddleware`'s `is_auth_bypassed` allowlist (`pharos-server/src/middleware.rs`) was missing `Command::AuthCheck` - under `Protected`/`Scoped` tier (real production hub `pharos-01.iamrichardd.com` runs `Protected`), every `auth-check` from a not-yet-authenticated connection was short-circuited with `506:Authentication required` before it ever reached the code that verifies the signature, making the feature structurally self-defeating on any Protected/Scoped hub since `auth-check`'s entire purpose is to authenticate an unauthenticated connection statelessly. Reproduced directly: a fresh challenge signed with a real registered key, sent via the exact wire handshake the console uses, got `506` every time; server logs confirmed the command was received and parsed but `AuthManager::verify_with_fingerprint` never ran. Never caught by existing tests or `scripts/live-verify`'s harness because both only ever exercised `Open` tier, where this gate doesn't apply. Fixed: one-line addition of `Command::AuthCheck { .. }` to the allowlist, safe for the identical reason `Command::Auth` is already bypassed - `AuthCheck`'s handler still performs full Ed25519 signature verification before returning `200:Ok`, so an invalid signature or unregistered key is still rejected with `516`; this only lets the attempt reach that real check. Two new unit tests (Protected/Scoped tier) and three new integration tests (valid auth-check succeeds under both tiers; a wrong-signature auth-check still correctly returns `516`, confirming the fix didn't weaken verification) added, panel-reviewed before dispatch. Live-verified twice: against a disposable server actually running `Protected` tier (closing the exact tier-coverage gap that let this ship unnoticed), and end-to-end on the real production hub with a real registered key - the exact previously-failing handshake now returns `200:Ok`, a bad signature is still correctly rejected.
- [x] **Debt #56 (Issue #203):** Action: added an unattended `pharos-scan --auto` device-discovery mode - discovers LAN devices that don't run `pharos-pulse` (IoT devices, switches, anything with no Pharos agent) and registers hostname/IP/MAC/manufacturer (OUI), nothing else (no port scanning, no role inference), on a 10-minute systemd timer. Full design in `artifacts/pharos-scan-periodic-discovery-plan.md`. Shipped in three independently-verified slices: (1) a general, server-derived `source` field (`mdb|ph|pharos-scan|pharos-pulse|web-console`, normalized from the connecting client's own `id`, never client-supplied - proven via an anti-spoofing test - immutable once set) plus three new Prometheus counters (`pharos_records_{added,updated,deleted}_total`, labeled by source) for alerting on anomalous write patterns; (2) replaced `pharos-scan`'s 6-entry hardcoded OUI stub with all three real IEEE MAC registries (~53,500 entries, vendored not fetched-at-build-time) - found and fixed a real bug while grounding this in actual data, the old prefix-matching assumed every entry was a fixed 3-octet block, wrong for the MA-M/MA-S registries covering exactly the newer/smaller vendors this data existed to add; (3) the `--auto` mode itself (ping-sweep via the system `ping` binary, ARP-cache read, concurrent OUI/reverse-DNS resolve, then a check-before-write sync that queries for an existing record first and skips entirely if it's owned by any source other than `pharos-scan` - proven end-to-end that a pulse-owned record's real manufacturer survives a conflicting scan attempt unchanged) plus a new opt-in `install.sh scan-auto` target (dedicated admin-role signing key, `Type=oneshot` service + timer, never enabled as a side effect of installing anything else). Live-verified at every layer: the compiled pipeline run for real against a real LAN (the `/20` subnet-size safety cap correctly excluded real leftover container bridge networks); the full `install.sh scan-auto` flow run inside a disposable, destroyed-afterward systemd-enabled container (real systemd unit parsing, real key permissions, the real binary completing with no permission errors as the unprivileged `pharos` user); and a full run against the real production hub, which surfaced and led to fixing one more real bug (`sync_discovered_device`'s query step needed `execute_authenticated()`, not plain `execute()` - the hub runs `SecurityTier::Protected`, where `query` requires auth unlike the `Open`-tier harness every prior test used) - after the fix, 9 real devices on the real LAN were correctly discovered and written, all pre-existing pulse-managed records stayed untouched, a second cycle correctly updated rather than duplicated them, and both new Prometheus counters matched exactly. Two known limitations carried forward, documented not overlooked: `source` doesn't survive multi-server SYNC replication, and there's no ownership-transfer path if a scan-discovered device is later adopted by `pharos-pulse` (operator workaround: delete the scan-created record first). The permanent systemd timer itself was deliberately not activated as part of this work - built, tested, and live-verified via manual invocation, left for a separate explicit decision given it's an ongoing standing change rather than one-time verification.
- [x] **Debt #57 (Issue #204):** Action: `mdb`/`ph` discarded the wire's parsed match count and printed every matched record's fields as one undifferentiated stream, with no visual separation between records. Confirmed live via `mdb '*' return hostname ip_addr` against 9 pharos-scan-discovered devices with no `hostname` field: the output was indistinguishable from a single record with 9 `ip_addr:` lines. Fixed: extracted a shared `render_matches()` helper into `pharos-cli-support` (this rendering logic was independently duplicated in both `mdb` and `ph`) - prints an "N matches:" header and blank-line separators between records' field blocks when there is more than one record; single-record and zero-record output unchanged, since mdb/ph's plain-text output is a real interface other scripts may already parse. Panel-reviewed twice (initial draft - duplication/testability findings - then the remediated extraction, which surfaced and fixed a doc-comment referencing migration history instead of current behavior). 4 new unit tests cover empty/single/multi-record and the `-H`/`--human` formatting callback. Shipped in v1.12.1.
- [x] **Debt #58 (Issue #205):** Action: v1.12.1's release commit bumped only `scripts/install.sh`'s `VERSION` by hand instead of using `scripts/bump-version.sh` (which exists for exactly this reason), leaving `mdb/Cargo.toml`, `ph/Cargo.toml`, and `crates/pharos-pulse/Cargo.toml` still at `1.12.0`. Caught during v1.12.1's mandatory live-download verification: the published `mdb`/`ph` binaries reported `--version` as `mdb 1.12.0` / `ph 1.12.0` despite the release being tagged `v1.12.1`. Fixed in v1.12.2: ran `scripts/bump-version.sh 1.12.2` to bump all four version-bearing files in lockstep, regenerated `Cargo.lock` in Podman, full workspace test suite (236 tests) confirmed passing. Live-verified: the downloaded v1.12.2 `mdb`/`ph` binaries now correctly report `--version` as `1.12.2`.
- [x] **Debt #59 (Issue #206):** Action: Implementation Team ran `pharos-scan --auto` against real production data on `pharos-01.iamrichardd.com` and found it created 8 confirmed duplicate `Machine` records for devices already tracked by `pharos-pulse`, inflating `pharos_total_records` from 10 to 19. Root cause: when a device's hostname can't be reverse-DNS-resolved (common on home networks without real PTR records), `sync_discovered_device()` fell back to an `alias=device-<mac>` identifier for its ownership check but never cross-referenced by `mac_addr` - the one field populated on both an alias-keyed scan record and a hostname-keyed pulse record for the same device - so the check found nothing and created a duplicate. Independently live-verified against the real hub before touching anything: cross-checked all 9 new `pharos-scan`-sourced records' MACs against every `pharos-pulse`-owned MAC by hand, which caught the original report overclaiming - one of the 9 (`device-90cafa72f5e4`, the LAN gateway) was a genuine new discovery, not a duplicate, so only the other 8 are actual duplicates worth removing. Cleanup of those 8 records was attempted but not completed from this environment: the only available key returned `516: No authorization for request`, since these records are owned by the `pharos-scan` signing key's fingerprint, which isn't present on this host - cleanup still needs to happen from wherever that key actually lives. Fixed: `sync_discovered_device()` now cross-references by `mac_addr` before falling back to alias-based creation when hostname resolution failed. Separately, `lookup_hostname()` (the reverse-DNS step) silently swallowed every failure into `None` with zero logging, making a real device's missing hostname indistinguishable from "no PTR record" versus a transient resolver failure - confirmed live investigating one of the 9 records, whose IP successfully reverse-resolves right now via both `host` and the NSS path `dns_lookup::lookup_addr` uses, meaning the scan-time failure was almost certainly transient and previously undiagnosable; added debug-level logging distinguishing the two cases. New integration test reproduces the exact production scenario. Uncovered and led to fixing a separate, more severe pre-existing bug - see Debt #60 (Issue #208) - since the MAC cross-reference query this fix depends on didn't actually work until that was fixed too.
- [x] **Debt #60 (Issue #208):** Action: found while implementing Debt #59 (Issue #206) - the new MAC cross-reference regression test failed, live-diagnosed (not guessed) down to `pharos-server/src/storage.rs`'s `matches()`: it splits *stored* field values on colon (among other delimiters) but never splits the *query* value the same way, so any colon-containing exact-value query (MAC addresses, IPv6 addresses) fragments the stored value into pieces that can never equal the unsplit query string. Confirmed live: adding a record with `mac_addr="bc:24:11:00:02:04"` and querying that exact value returned zero matches despite the record genuinely existing - meaning `cli-clients.mdx`'s own documented example (`mdb mac_addr="e0:51:d8:1d:e3:22"`) has likely never worked as advertised, silently, with no error (a `mdb`/`ph` empty result looks identical to a genuine "no matches"). Design ("use the typed datatypes, not raw strings") came from the user; reviewed by the panel before implementation. Fixed: `ip_addr`/`mac_addr` matching now parses both sides into `std::net::IpAddr` or 6 raw MAC bytes (a small hand-rolled parser reusing the existing validator's logic - no new dependency) and compares by typed equality, falling back to the original `matches()` only when the query value isn't a parseable address (preserving existing wildcard-search support for colon-free patterns). Bonus correctness fix: also makes MAC comparison separator- and case-insensitive and IPv6 comparison compression-form-insensitive. 4 new unit tests. Independently re-verified, not just trusted from the builder's report: caught the builder silently substituting a weaker test (`mac_addr="bc"`, not a wildcard) for the plan-specified wildcard regression test (`mac_addr="bc:24:*"`) without disclosing the mismatch as instructed - live-confirmed the plan's original test genuinely fails, revealing a further, distinct residual bug filed separately as Debt #61 (Issue #207) rather than silently masked. Full test suite re-run independently after the fix (109 `pharos-server` unit tests, 7 `pharos-scan` integration tests) before considering this done.
- [ ] **Debt #61 (Issue #207):** Action: found while fixing Debt #60 (Issue #208) - wildcard `mac_addr`/`ip_addr` search patterns that themselves contain a colon (e.g. `mac_addr="bc:24:*"`) match nothing, even against a record that should match, due to the identical stored-value-splits-but-query-doesn't asymmetry as Debt #60, just manifesting in the `matches()` fallback path instead of the typed-comparison path. A colon-free fragment query (`mac_addr="bc"`) still works fine. Deliberately not bundled into Debt #60's fix - needs its own design pass (likely making the wildcard-pattern matching colon-aware for these two fields specifically) rather than a quick patch, since `wildcard_match`/`matches()` are shared by every field type in the system.
